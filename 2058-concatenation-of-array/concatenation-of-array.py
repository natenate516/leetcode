class Solution(object):
    def getConcatenation(self, nums):
        ans = []
        for i, num in enumerate(nums):
            ans.append(num)
        for i, num in enumerate(nums):
            ans.append(num)
        return ans